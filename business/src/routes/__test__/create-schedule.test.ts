import request from 'supertest';
import { app } from '../../app';

// ADDING SCHEDULE

// success

// checks for every missing field

// business doesn't exist
it('returns HTTP Error Code 404 (Not Found)')

it('returns HTTP Error Code 201 (Created) for schedule creation', async () => {

});