version: 2.1

orbs:
  node: circleci/node@4.0.0

jobs:
  backend-build-test:
    executor:
      name: node/default
      tag: '13'
    steps:
      - checkout
      - node/install-packages
      - run: npm test
  frontend-build-test:
    executor:
      name: node/default
      tag: '13'
    steps:
      - checkout
      - node/install-packages
      - run: npm test

workflows:
  automated-testing:
    jobs:
      - backend-build-test:
        filters:
          branches:
            ignore:
              - master
      - frontend-build-test:
        filters:
          branches:
            ignore:
              - master
